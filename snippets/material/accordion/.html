<mat-accordion displayMode="${1:default|flat}" [multi]="${2:true|false}"
               [hideToggle]="${3:false|true}">
    <mat-expansion-panel [hideToggle]="${4:false|true}">
        <mat-expansion-panel-header>${7:Panel header}</mat-expansion-panel-header>
        ${8: Panel body}$END$
        <mat-action-row>
            <button mat-button (click)="${6:onAction($event)}">${5:Action1}</button>
        </mat-action-row>
    </mat-expansion-panel>
</mat-accordion>
